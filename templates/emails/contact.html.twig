<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">   
    </head>
    <body>
        <h1> Demande de {{contact.fullName }}</h1>

        <p> Email : {{contact.email }}</p>
        <p> Objet : {{contact.objet }}</p>
        <p> Message : </p>
        <div>
        {{contact.message |raw}}
        </div>
        
    </body> 
        
</html>
